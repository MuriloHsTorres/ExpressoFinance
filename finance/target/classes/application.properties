# --- Configura��o do Banco de Dados MySQL ---
# Vamos usar o nome 'expressodb' como no seu escopo.
# O 'createDatabaseIfNotExist=true' � muito �til: ele cria o banco automaticamente se n�o existir.
spring.datasource.url=jdbc:mysql://localhost:3306/expressodb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# !!! MUITO IMPORTANTE !!!
# Coloque aqui o seu usu�rio e senha do MySQL. O 'root' � o padr�o.
spring.datasource.username=root
spring.datasource.password=1234

# --- Configura��o do JPA (Hibernate) ---
# Isso � m�gico: 'update' diz ao Spring para olhar suas classes Java
# (que vamos criar) e criar/atualizar as tabelas no banco automaticamente.
spring.jpa.hibernate.ddl-auto=update

# Isso mostra no console o SQL que o Spring est� executando (�timo para debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect